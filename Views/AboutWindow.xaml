<Window x:Class="FightingTournament.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Loc_AboutTitle}"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        Foreground="{DynamicResource BrushText}"
        ResizeMode="NoResize"
        WindowStyle="None">

    <Border Background="{DynamicResource BrushBg}">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="1,0,1,1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="2"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource BrushAccentDim}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title / Icon -->
            <RowDefinition Height="*"/>    <!-- Text info / credits -->
            <RowDefinition Height="Auto"/> <!-- Close Button -->
        </Grid.RowDefinitions>

        <!-- Gradients border on top of the window -->
        <Border Height="3" VerticalAlignment="Top" Margin="-20,-20,-20,0" Grid.Row="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{DynamicResource ColorBg}" Offset="0.0"/>
                    <GradientStop Color="{DynamicResource ColorAccent}" Offset="0.5"/>
                    <GradientStop Color="{DynamicResource ColorBg}" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Icon & Title -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,15,0,15">
            <TextBlock Text="⚔" FontSize="36" Foreground="{DynamicResource BrushAccent}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Text="{DynamicResource Loc_AppTitle}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource BrushText}" HorizontalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Loc_AboutVersion}" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextDim}" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Text Description & Credits -->
        <StackPanel Grid.Row="1" Margin="10,0,10,20">
            <TextBlock Text="{DynamicResource Loc_AboutDescription}"
                       FontSize="12"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Foreground="{DynamicResource BrushText}"
                       Margin="0,0,0,18"
                       LineHeight="18"/>

            <Border Background="{DynamicResource BrushSurface2}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Padding="12" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Loc_AboutKeyAdvancements}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource BrushAccent}" Margin="0,0,0,6"/>
                    <TextBlock Text="{DynamicResource Loc_AboutAdvFileSwapping}" FontSize="11" Foreground="{DynamicResource BrushText}" Margin="0,0,0,4"/>
                    <TextBlock Text="{DynamicResource Loc_AboutAdvPngExporters}" FontSize="11" Foreground="{DynamicResource BrushText}" Margin="0,0,0,4"/>
                    <TextBlock Text="{DynamicResource Loc_AboutAdvPresetsManager}" FontSize="11" Foreground="{DynamicResource BrushText}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="2"
                Content="{DynamicResource Loc_Close}"
                Style="{StaticResource BtnPrimary}"
                Padding="30,8"
                FontSize="11"
                HorizontalAlignment="Center"
                Click="CloseButton_Click"/>
    </Grid>
    </Border>
</Window>
